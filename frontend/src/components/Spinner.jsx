import spinner from '../img/spinner.gif';

const Spinner = () => {
    return (
        <img src={spinner} alt="Spinner" style={{ width: '200px', margin: 'auto', display: 'block', paddingTop: '50px' }}  />
    )
}

export default Spinner;
